# ClipFlow 仕様書
## 動画合成デスクトップアプリ（Electron）

---

## 1. 概要

### 1.1 アプリ種別
- デスクトップアプリケーション（Electron 28）
- 対象OS：Windows（メイン）
- 将来的に macOS 対応可能な設計

### 1.2 目的
- 動画編集ソフトを使わずに、**セグメントベースの編集**により、シーンごとに異なるレイアウトを適用可能
- IN/OUT調整、クロップ、音声ミキシング、オーバーレイを一画面で完結

### 1.3 技術スタック
| レイヤー | 技術 |
|---------|------|
| フロントエンド | React 18 + TypeScript + Vite |
| デスクトップ | Electron 28 |
| 状態管理 | Zustand |
| スタイリング | Tailwind CSS |
| 動画処理 | FFmpeg (fluent-ffmpeg) |
| 音声処理 | Tone.js (Web Audio API) |
| パッケージング | electron-builder (NSIS) |

---

## 2. 想定ユーザー

- TikTok / YouTube Shorts / Reels 投稿者
- 比較動画、リアクション動画、対談、実況動画制作者
- 編集は最小限で済ませたいが、トリミング精度と操作感にはこだわりたいユーザー

---

## 3. 基本コンセプト

- ローカル完結（アップロード不要、オフライン可）
- セグメント駆動（シーンごとにレイアウト変更可能）
- スライダー中心のUI（数値入力は補助）
- 1画面完結型UI
- リアルタイムプレビュー対応

---

## 4. データモデル

### 4.1 クリップ（Clip）
個別のメディアファイルを表す

```typescript
{
  id: string
  filePath: string           // ファイルパス
  fileName: string           // ファイル名
  duration: number           // 総再生時間（秒）
  width: number              // 横解像度
  height: number             // 縦解像度
  fps: number                // フレームレート
  inPoint: number            // トリム開始点（秒）
  outPoint: number           // トリム終了点（秒）
  thumbnails: string[]       // サムネイル（Base64）
  cropX: number              // クロップX位置（-1.0〜1.0）
  cropY: number              // クロップY位置（-1.0〜1.0）
  cropScale: number          // ズーム倍率（0.5〜2.0）
  pitchShift: number         // ピッチシフト（0〜-7半音）
}
```

### 4.2 エントリー（Entry）
セグメント内でのクリップ使用を表す

```typescript
// メインエントリー
MainEntry {
  clipId: string
  inPoint: number            // クリップ内の開始位置
  duration: number           // 再生時間
}

// サブエントリー
SubEntry {
  clipId: string
  inPoint: number
  duration: number
}
```

### 4.3 セグメント（Segment）
出力動画内の1シーンを表す

```typescript
{
  id: string
  layoutType: LayoutType     // レイアウト種別
  duration: number           // セグメント時間（mainEntries合計）
  mainEntries: MainEntry[]   // メインレーンのエントリー（最大10）
  subEntries: SubEntry[]     // サブレーンのエントリー（最大10）

  // PiP設定
  pipPosition?: 'bottom-right' | 'bottom-left' | 'top-right' | 'top-left'
  pipSize?: '1/4' | '1/3' | '1/5'
  pipOrientation?: 'horizontal' | 'vertical'

  // オーバーレイ
  mainImageOverlay?: ImageOverlay
  subImageOverlay?: ImageOverlay
  textTelop?: TextTelopSettings

  // 音声設定（セグメント単位上書き）
  mainVolume?: number
  subVolume?: number

  // フィットモード
  mainFitMode?: 'cover' | 'contain'
  subFitMode?: 'cover' | 'contain'
}
```

### 4.4 レーン（Lane）
クリップを格納するトラック

```typescript
{
  id: 'main' | 'sub'
  clips: Clip[]              // 最大10クリップ
}
```

### 4.5 プロジェクト（Project）
保存・読み込み単位

```typescript
{
  version: number
  mainLane: Lane
  subLane: Lane
  segments: Segment[]
  aspectRatio: '16:9' | '9:16'
  audioBalance: number       // 0-100
  mainVolume: number         // 0-200
  subVolume: number          // 0-200
  bgm?: BGMSettings
}
```

---

## 5. レイアウト仕様

### 5.1 レイアウト種別

| 種別 | 説明 | メイン | サブ |
|------|------|--------|------|
| `single-main` | フルスクリーン | 全画面 | 非表示 |
| `split-h` | 左右分割 | 左半分 | 右半分 |
| `split-v` | 上下分割 | 上半分 | 下半分 |
| `split-3h` | 3分割（横） | 左1/3 | 中央・右1/3 |
| `pip` | ピクチャーインピクチャー | 全画面 | 小窓 |

### 5.2 出力解像度

#### 16:9（横長）- 1920x1080
| レイアウト | メインサイズ | サブサイズ |
|-----------|-------------|-----------|
| single-main | 1920x1080 | - |
| split-h | 960x1080 | 960x1080 |
| split-v | 1920x540 | 1920x540 |
| split-3h | 640x1080 | 640x1080 x2 |
| pip (1/4) | 1920x1080 | 480x270 |
| pip (1/3) | 1920x1080 | 640x360 |
| pip (1/5) | 1920x1080 | 384x216 |

#### 9:16（縦長）- 1080x1920
| レイアウト | メインサイズ | サブサイズ |
|-----------|-------------|-----------|
| single-main | 1080x1920 | - |
| split-h | 540x1920 | 540x1920 |
| split-v | 1080x960 | 1080x960 |
| split-3h | 360x1920 | 360x1920 x2 |
| pip (1/4) | 1080x1920 | 270x480 |
| pip (1/3) | 1080x1920 | 360x640 |
| pip (1/5) | 1080x1920 | 216x384 |

### 5.3 PiP設定

- **位置**: 4隅（bottom-right, bottom-left, top-right, top-left）
- **サイズ**: 1/4, 1/3, 1/5
- **向き**: horizontal（横長）/ vertical（縦長）
- **マージン**: 40px固定

### 5.4 フィットモード

| モード | 説明 |
|--------|------|
| cover | アスペクト比維持、はみ出し部分をクロップ（デフォルト） |
| contain | アスペクト比維持、全体表示（黒帯あり） |

---

## 6. 入力仕様

### 6.1 対応形式

| 種別 | 形式 |
|------|------|
| 動画 | MP4, MOV |
| 音声 | MP3, WAV, AAC, FLAC, M4A, OGG |
| 画像 | PNG, JPG, JPEG, GIF, WebP, BMP |

### 6.2 取り込み方法
- ドラッグ＆ドロップ
- ファイル選択ダイアログ

### 6.3 動画情報取得
- 動画尺（duration）
- 解像度（width, height）
- フレームレート（fps）
- サムネイル自動生成

---

## 7. トリム（IN/OUT）仕様

### 7.1 基本仕様
- 各クリップにIN（開始時間）とOUT（終了時間）を設定
- 制約: `0 <= IN < OUT <= duration`

### 7.2 UI要件

#### レンジスライダー
- 2ハンドル型スライダー（IN/OUT）
- ドラッグ操作で直感的に調整

#### プレビュー連動
- ハンドル操作時に該当フレームを表示

#### 精密操作
- 数値入力欄（秒 / mm:ss）
- キーボード操作: ←/→で±0.1秒、Shift+←/→で±1秒

#### スナップ（吸着）
- 0秒、動画末尾、0.5秒グリッドに吸着

### 7.3 サムネイルストリップ
- 動画全体を表す複数サムネイルを横並び表示
- 選択範囲をハイライト

---

## 8. クロップ・ズーム仕様

### 8.1 パラメータ

| 設定 | 範囲 | デフォルト | 説明 |
|------|------|-----------|------|
| cropX | -1.0〜1.0 | 0 | 水平位置 |
| cropY | -1.0〜1.0 | 0 | 垂直位置 |
| cropScale | 0.5〜2.0 | 1.0 | ズーム倍率 |

### 8.2 操作
- ドラッグでパン位置調整
- スライダーでズーム倍率調整
- リアルタイムプレビュー

---

## 9. 音声仕様

### 9.1 グローバル音声設定

| 設定 | 範囲 | デフォルト | 説明 |
|------|------|-----------|------|
| mainVolume | 0-200% | 100% | メインレーン音量 |
| subVolume | 0-200% | 100% | サブレーン音量 |
| audioBalance | 0-100 | 50 | 音声バランス（0=メインのみ、50=等分、100=サブのみ） |

### 9.2 ピッチシフト

| 設定 | 範囲 | デフォルト | 説明 |
|------|------|-----------|------|
| pitchShift | 0〜-7半音 | 0 | 声の高さ調整 |

- -1〜-3: 微妙な変化
- -5〜-7: 顕著な変化

### 9.3 BGM設定

| 設定 | 範囲 | デフォルト | 説明 |
|------|------|-----------|------|
| volume | 0.0-2.0 | 0.5 | BGM音量 |
| fadeIn | 0-10秒 | 0 | フェードイン時間 |
| fadeOut | 0-10秒 | 0 | フェードアウト時間 |

- 動画より短い場合は自動ループ
- 動画・音声ファイルから読み込み可能

---

## 10. オーバーレイ仕様

### 10.1 画像オーバーレイ

| 設定 | 範囲 | デフォルト | 説明 |
|------|------|-----------|------|
| positionX | -1.0〜1.0 | 0 | 水平位置 |
| positionY | -1.0〜1.0 | 0 | 垂直位置 |
| size | 5-200% | 100% | 出力幅に対するサイズ比 |

- 対応形式: PNG, JPG, GIF, WebP, BMP
- メイン/サブそれぞれに設定可能

### 10.2 テキスト/テロップオーバーレイ

| 設定 | オプション | デフォルト |
|------|-----------|-----------|
| position | top, center, bottom | bottom |
| fontSize | small, medium, large | medium |
| fontFamily | sans-serif, serif | sans-serif |
| textColor | white, black | white |
| background | true, false | true |

---

## 11. プレビュー仕様

### 11.1 リアルタイムプレビュー
- 合成結果のリアルタイム表示
- 再生/一時停止
- シークバー（0〜出力尺）
- 現在時間表示

### 11.2 音声プレビュー
- Tone.jsによるリアルタイム音声処理
- ピッチシフト適用プレビュー
- 音量・バランス適用

---

## 12. 書き出し仕様

### 12.1 出力設定

| 項目 | 値 |
|------|-----|
| 形式 | MP4 |
| 映像コーデック | H.264 |
| 音声コーデック | AAC |
| 映像品質 | CRF 23 |
| 音声ビットレート | 192kbps |
| プリセット | fast |

### 12.2 書き出し機能
- 進捗バー表示（%）
- キャンセル機能
- 入力ファイルと同じパスへの出力対応（一時ファイル経由）

### 12.3 保存先
- デフォルト: ダウンロードフォルダ
- 任意で変更可能

---

## 13. プロジェクト管理

### 13.1 ファイル操作

| 操作 | ショートカット | 説明 |
|------|---------------|------|
| 新規 | Ctrl+N | 空のプロジェクト作成 |
| 開く | Ctrl+O | .veprojファイルを開く |
| 保存 | Ctrl+S | .veprojファイルに保存 |

### 13.2 プロジェクトファイル
- 拡張子: `.veproj`
- 形式: JSON
- メディアファイルはパス参照（埋め込みなし）

---

## 14. UI構成

```
┌─────────────────────────────────────────┐
│ ヘッダー                                │
│ - アスペクト比選択 (16:9 / 9:16)       │
│ - グローバル音量コントロール            │
│ - 音声バランススライダー                │
│ - BGMコントロール                       │
├─────────────────────────────────────────┤
│ プレビューセクション                    │
│ - ライブ動画プレビュー                  │
│ - 再生コントロール                      │
│ - シークバー                            │
│ - 時間表示                              │
├─────────────────────────────────────────┤
│ セグメントパネル                        │
│ - セグメントタブ（タイムライン）        │
│ - メインクリップリスト                  │
│ - サブクリップリスト                    │
│ - レイアウト選択                        │
│ - PiP設定                               │
│ - オーバーレイ設定                      │
│ - トリムエディター                      │
│ - クロップエディター                    │
├─────────────────────────────────────────┤
│ フッター                                │
│ - 総出力時間表示                        │
│ - エクスポートボタン                    │
└─────────────────────────────────────────┘
```

---

## 15. 制約・制限

| 項目 | 制限 |
|------|------|
| レーンあたりクリップ数 | 最大10 |
| セグメントあたりエントリー数 | 最大10 |
| 音量ブースト | 最大200% |
| ピッチシフト | 0〜-7半音 |
| PiPマージン | 40px固定 |

---

## 16. エラー処理

### 16.1 エラー種別
- ファイル形式非対応
- 動画読み込み失敗
- IN/OUT不正値
- 書き込み不可（権限・容量）
- FFmpeg実行エラー
- 入出力パス競合

### 16.2 UI要件
- エラー内容を日本語で明示
- 設定を保持したまま再試行可能
- 詳細ログを確認可能

---

## 17. 非機能要件

### 17.1 パフォーマンス
- UI操作は常に快適（60fps目標）
- スライダー操作中はプレビュー更新を間引く

### 17.2 セキュリティ
- ネットワーク通信なし（ローカル完結）
- ファイル操作はユーザー操作起点のみ

### 17.3 保守性
- ログ出力あり
- 設定はローカル保存

---

## 18. 今後の拡張候補

- トランジション効果
- テキストアニメーション
- エフェクトフィルター
- プリセット保存
- macOS正式対応
- ハードウェアエンコード対応

---

## 変更履歴

| バージョン | 日付 | 内容 |
|-----------|------|------|
| v1.0 | - | 初版（左右分割のみ） |
| v2.0 | 2026-03-18 | セグメントベース、複数レイアウト、PiP、オーバーレイ、BGM対応 |
