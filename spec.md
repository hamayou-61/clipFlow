# 要件定義書 v1.0  
## スプリット動画合成デスクトップアプリ（Electron）

---

## 1. 概要

### 1.1 アプリ種別
- デスクトップアプリケーション（Electron）
- 対象OS：Windows（メイン）
- 将来的に macOS 対応可能な設計

### 1.2 目的
- 動画編集ソフトを使わずに、
  **左右2分割 × 各レーン複数クリップ連結**
  の動画を、直感的で気持ちの良いUI/UXで作成できるようにする
- 特に **IN / OUT をスライダーで調整する体験**を最重要視する

---

## 2. 想定ユーザー

- TikTok / YouTube Shorts / Reels 投稿者
- 比較動画、リアクション動画、対談、実況動画制作者
- 編集は最小限で済ませたいが、トリミング精度と操作感にはこだわりたいユーザー

---

## 3. 基本コンセプト

- ローカル完結（アップロード不要、オフライン可）
- テンプレート駆動（自由編集より「迷わない設計」）
- スライダー中心のUI（数値入力は補助）
- 1画面完結型UI

---

## 4. 動画構成仕様

### 4.1 レーン構成（可変）

- レーンは **左右2レーン固定**
  - 左レーン
  - 右レーン

- 各レーンには **1本以上の動画クリップを順番に連結**できる
- UI上の並び順が、そのまま再生順となる

#### デフォルト状態
- 左レーン：1クリップ
- 右レーン：1クリップ
- 合計2クリップから開始（初心者を迷わせない）

#### 追加仕様
- 各レーンに「＋クリップ追加」ボタンを配置
- クリックで新しいクリップスロットを追加

---

### 4.2 動画数の制約

- 各レーンの動画数：
  - 最小：1本
  - 最大：5本
- 全体最大数：
  - 最大10本（左右合計）

※ v1.0ではUI・パフォーマンス・FFmpeg処理の安定性を考慮し最大5本とする

---

## 5. 入力仕様

### 5.1 対応形式
- mp4 / mov（MVP）
- 音声あり・なし両対応

### 5.2 取り込み方法
- ドラッグ＆ドロップ
- ファイル選択ダイアログ

### 5.3 動画情報取得
- 動画尺（duration）
- 解像度
- フレームレート（取得可能な場合）

---

## 6. トリム（IN / OUT）仕様【最重要】

### 6.1 基本仕様
- 各クリップに以下を設定可能
  - IN（開始時間）
  - OUT（終了時間）
- 制約条件  
  - `0 <= IN < OUT <= 動画尺`

---

### 6.2 UI要件（必須）

#### 6.2.1 レンジスライダー
- 2ハンドル型スライダー（IN / OUT）
- 単位：秒（内部表現はミリ秒可）
- ドラッグ操作で直感的に調整可能

#### 6.2.2 プレビュー連動
- INハンドル操作中：IN時点のフレームを表示
- OUTハンドル操作中：OUT時点のフレームを表示
- 表示は静止画（低解像度で可）

#### 6.2.3 精密操作
- 数値入力欄（秒 / mm:ss）
- キーボード操作
  - ← / →：±0.1秒
  - Shift + ← / →：±1秒

#### 6.2.4 スナップ（吸着）
- 初期状態で有効
- 吸着対象
  - 0秒
  - 動画末尾
  - 疑似グリッド（例：0.5秒刻み）

---

### 6.3 サムネイルストリップ
- 動画全体を表す複数サムネイルを横並び表示
- スライダーの選択範囲を視覚的にハイライト
- サムネイル生成はバックグラウンドで実行

---

## 7. レーン尺・全体尺の扱い

### 7.1 レーン尺
- レーン尺 = 各クリップの `(OUT - IN)` の合計

### 7.2 出力尺（v1.0固定仕様）
- 出力尺 = `min(左レーン尺, 右レーン尺)`
- 長いレーンは **後方からカット**
  - 最後のクリップ内でカットされる可能性あり

---

## 8. レイアウト・出力仕様

### 8.1 分割方式
- 左右分割のみ

### 8.2 出力アスペクト比
- 16:9（1920 × 1080）
- 9:16（1080 × 1920）

### 8.3 フィット方式
- Cover固定（はみ出しはトリミング）

---

## 9. 音声仕様

### 9.1 音声モード
- 左レーンのみ
- 右レーンのみ
- ミックス（50:50、-6dBずつ）

### 9.2 初期値
- 左レーンのみ

---

## 10. プレビュー仕様

### 10.1 合成プレビュー
- 出力結果の左右合成プレビュー（静止画）
- 再生位置スライダー（0〜出力尺）
- 再生位置に応じて
  - 各レーン内の該当クリップフレームを使用

---

## 11. 書き出し仕様

- 出力形式：mp4
- コーデック：H.264 + AAC
- フレームレート：入力に追従（難しければ30fps固定）
- 保存先：
  - デフォルト：ダウンロードフォルダ
  - 任意で変更可能
- ファイル名：
  - `split_YYYYMMDD_HHMMSS.mp4`

---

## 12. 進捗・制御

- 書き出し進捗バー（％）
- 状態管理
  - 待機 / 処理中 / 完了 / 失敗 / キャンセル
- キャンセル時
  - FFmpeg処理停止
  - 中間ファイル削除

---

## 13. エラー処理

### 13.1 エラー種別
- ファイル形式非対応
- 動画読み込み失敗
- IN / OUT 不正
- 書き込み不可（権限・容量）
- FFmpeg実行エラー

### 13.2 UI要件
- エラー内容を日本語で明示
- 設定を保持したまま再試行可能
- 詳細ログを確認可能

---

## 14. 非機能要件

### 14.1 パフォーマンス
- UI操作は常に快適（60fps目標）
- スライダー操作中はプレビュー更新を間引く（最大10fps程度）

### 14.2 セキュリティ
- ネットワーク通信なし（ローカル完結）
- ファイル操作はユーザー操作起点のみ

### 14.3 保守性
- ログ出力あり
- 設定はローカル保存（前回設定を復元）

---

## 15. スコープ外（v1.0では実装しない）

- 上下分割
- 動画プレビュー再生（リアルタイム）
- レーンを跨いだクリップ移動
- ループ / 黒埋めによる尺合わせ
- 位置調整（トリミング位置指定）
- 課金・クラウド連携

---

## 16. 今後の拡張候補

- 簡易動画プレビュー
- プリセット保存
- テンプレ追加（ワイプ、小窓）
- 音量調整
- macOS正式対応

---
